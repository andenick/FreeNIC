# --- Data artifacts (served from data.freenic.org, never committed) ---
*.duckdb
*.duckdb.tmp
*.duckdb.wal
*.parquet
*.zip

# The build-host warehouse / raw acquisition dirs
Outputs/
Inputs/
# Build-host input fallback root (DATA_ROOT): bulk FFIEC ZIPs + the unused
# freenic.duckdb.STUB_UNUSED scaffolding stub + its operational README.
# Never part of the public repo. (Added 2026-07-16, FIN-1.)
data/

# Internal operational workspace — NEVER part of the public repo.
# Holds review reports, handoffs, state files, HDARP processing, the MCP
# stack, snapshots, and internal (Council/*) references. Public deliverables
# live under pipeline/, site/, tools/, release-tools/. (Added 2026-07-16 to
# stop the 915 untracked Technical/ items from ever being accidentally staged.)
Technical/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# --- Secrets / local config ---
.env
.env.*
!.env.example
keys.env
*.key

# --- Editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Backups / snapshots ---
*.bak
*.bak-*
.snapshots/
